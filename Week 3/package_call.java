import tools.*;
public class package_call {
    //package call is a way to access classes and methods from other packages.
    //To use it, you need to import the desired package using the "import" keyword followed
    //by the name of the package. Then, you can create an object of the class or
    public static void main(String[] args) {
        package_intro package_intro=new package_intro();
        package_intro.intro();
    }
}
